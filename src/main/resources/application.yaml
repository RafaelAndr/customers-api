spring:
  application:
    name: customerportal
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:coder-bank-client
    username: user
    password: user
    driverClassName: org.h2.Driver